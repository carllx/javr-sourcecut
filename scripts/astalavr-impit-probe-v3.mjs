#!/usr/bin/env node

// Browser Lead standalone AstalaVR transport probe v3.
// Intentionally independent of the project's TypeScript build/typecheck.
import fs from 'node:fs';
import fsp from 'node:fs/promises';
import os from 'node:os';
import path from 'node:path';
import { spawn, spawnSync } from 'node:child_process';
import { createRequire } from 'node:module';
import { pathToFileURL } from 'node:url';
const TARGET = process.argv[2] || 'https://astalavr.com/videos/qDAVn/tmavr285-jun-suehiro-oguri-misao';
const PROFILE_DIR = process.env.JAVR_PROFILES_DIR ? path.join(process.env.JAVR_PROFILES_DIR, 'astalavr') : path.join(process.env.LOCALAPPDATA || path.join(os.homedir(), '.javr-sourcecut'), 'javr-sourcecut', 'profiles', 'astalavr');
const IMPIT_VERSION = '0.14.3';
const red=(code,detail)=>{console.error('RESULT=RED');console.error(`FAILURE=${code}`);if(detail)console.error(String(detail));process.exitCode=1;};
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const cookieNames=h=>!h?[]:h.split(';').map(x=>x.trim().split('=',1)[0]).filter(Boolean).sort();
const safeUrl=raw=>{const u=new URL(raw);return `${u.origin}${u.pathname}`;};
function findBrowser(){if(process.env.JAVR_BROWSER_PATH&&fs.existsSync(process.env.JAVR_BROWSER_PATH))return process.env.JAVR_BROWSER_PATH;const c=process.platform==='win32'?['C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe','C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe',path.join(process.env.LOCALAPPDATA||'','Google','Chrome','Application','chrome.exe'),'C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe','C:\\Program Files\\Microsoft\\Edge\\Application\\msedge.exe']:['/Applications/Google Chrome.app/Contents/MacOS/Google Chrome','/usr/bin/google-chrome','/usr/bin/chromium','/usr/bin/chromium-browser','/Applications/Microsoft Edge.app/Contents/MacOS/Microsoft Edge','/usr/bin/microsoft-edge'];return c.find(p=>p&&fs.existsSync(p))||null;}
async function readExistingProfileCookies(){if(!fs.existsSync(PROFILE_DIR))throw new Error(`Dedicated AstalaVR profile does not exist: ${PROFILE_DIR}`);if(typeof WebSocket!=='function')throw new Error(`Node ${process.version} does not expose global WebSocket; probe requires Node 22+.`);const browserPath=findBrowser();if(!browserPath)throw new Error('Chrome/Edge executable not found. No login attempt was made.');const activePortFile=path.join(PROFILE_DIR,'DevToolsActivePort');await fsp.rm(activePortFile,{force:true}).catch(()=>{});const proc=spawn(browserPath,[`--user-data-dir=${PROFILE_DIR}`,'--remote-debugging-port=0','--remote-debugging-address=127.0.0.1','--headless=new','--no-first-run','--no-default-browser-check','about:blank'],{stdio:'ignore',windowsHide:true});let exited=false,exitCode=null;proc.once('exit',c=>{exited=true;exitCode=c;});let port=0,wsPath='';try{const deadline=Date.now()+8000;while(Date.now()<deadline){try{const text=await fsp.readFile(activePortFile,'utf8');const lines=text.trim().split(/\r?\n/);if(lines.length>=2){port=Number(lines[0]);wsPath=lines[1];if(port>0&&wsPath)break;}}catch{}if(exited)throw new Error(`Profile browser exited before CDP became available (exit=${exitCode}). Another Chrome process may still own the profile.`);await sleep(100);}if(!port||!wsPath)throw new Error('Timed out opening existing profile for read-only CDP cookie extraction. No login attempt was made.');const ws=new WebSocket(`ws://127.0.0.1:${port}${wsPath}`);await new Promise((resolve,reject)=>{const timer=setTimeout(()=>reject(new Error('CDP WebSocket open timed out')),4000);ws.onopen=()=>{clearTimeout(timer);resolve();};ws.onerror=()=>{clearTimeout(timer);reject(new Error('CDP WebSocket connection failed'));};});const send=(id,method,params={})=>new Promise((resolve,reject)=>{const timer=setTimeout(()=>reject(new Error(`${method} timed out`)),4000);const handler=event=>{try{const msg=JSON.parse(event.data);if(msg.id!==id)return;ws.removeEventListener('message',handler);clearTimeout(timer);if(msg.error)reject(new Error(`${method}: ${msg.error.message||JSON.stringify(msg.error)}`));else resolve(msg.result||{});}catch{}};ws.addEventListener('message',handler);ws.send(JSON.stringify({id,method,params}));});const result=await send(101,'Storage.getCookies');const all=Array.isArray(result.cookies)?result.cookies:[];const cookies=all.filter(c=>{const d=String(c.domain||'').toLowerCase().replace(/^\./,'');return d==='astalavr.com'||d.endsWith('.astalavr.com');}).map(c=>({domain:String(c.domain||''),path:String(c.path||'/'),secure:Boolean(c.secure),expires:typeof c.expires==='number'?c.expires:0,name:String(c.name||''),value:String(c.value||'')})).filter(c=>c.name);try{ws.send(JSON.stringify({id:999,method:'Browser.close',params:{}}));}catch{}try{ws.close();}catch{}return cookies;}finally{const deadline=Date.now()+1500;while(!exited&&Date.now()<deadline)await sleep(50);if(!exited)try{proc.kill();}catch{}}}
function cookieMatches(c,raw){const u=new URL(raw),host=u.hostname.toLowerCase(),domain=c.domain.toLowerCase().replace(/^\./,'');if(!(host===domain||host.endsWith(`.${domain}`)))return false;if(c.secure&&u.protocol!=='https:')return false;if(!u.pathname.startsWith(c.path||'/'))return false;if(c.expires>0&&c.expires<Date.now()/1000)return false;return true;}
function cookieHeader(cookies,raw){const m=new Map();for(const c of cookies)if(cookieMatches(c,raw))m.set(c.name,c.value);return[...m].map(([k,v])=>`${k}=${v}`).join('; ');}
function parseLivePage(html){const id=new URL(TARGET).pathname.match(/\/videos\/([^/]+)/i)?.[1]||'unknown';const re=/<source\b[^>]*?src=["']([^"']+)["'][^>]*?>/gi,qre=/quality=["']([^"']+)["']/i,r=[];let m;while((m=re.exec(html))){const tag=m[0],src=m[1].replace(/&amp;/g,'&');if(!/^https?:\/\//i.test(src)||!/\.mp4(?:\?|$)/i.test(src))continue;const q=tag.match(qre)?.[1]||'unknown',n=Number((q.match(/(\d{3,4})/)||[])[1])||Number.MAX_SAFE_INTEGER;r.push({src,quality:q,numericQuality:n});}r.sort((a,b)=>a.numericQuality-b.numericQuality);return{id,renditions:r};}
function ensureImpit(){const root=path.join(os.tmpdir(),'javr-sourcecut-impit-probe');fs.mkdirSync(root,{recursive:true});const pkg=path.join(root,'package.json');if(!fs.existsSync(pkg))fs.writeFileSync(pkg,JSON.stringify({name:'javr-sourcecut-impit-probe',private:true,type:'module'},null,2));const req=createRequire(pkg);try{return req.resolve('impit');}catch{}console.log(`IMPIT_INSTALL=starting:${IMPIT_VERSION}`);let result;if(process.platform==='win32'){const npmCli=path.join(path.dirname(process.execPath),'node_modules','npm','bin','npm-cli.js');result=spawnSync(process.execPath,[npmCli,'install','--prefix',root,'--no-save',`impit@${IMPIT_VERSION}`],{stdio:'inherit',windowsHide:true});}else result=spawnSync('npm',['install','--prefix',root,'--no-save',`impit@${IMPIT_VERSION}`],{stdio:'inherit'});if(result.error||result.status!==0)throw new Error(result.error?.message||`temporary impit install exited ${result.status}`);return req.resolve('impit');}
async function main(){console.log(`TARGET=${TARGET}`);console.log('SECRET_LOGGING=disabled');console.log('MODE=standalone existing-profile page fetch + bytes=0-0 media probe only');console.log('PROBE_VERSION=v3-no-project-build');console.log(`PROFILE_DIR=${PROFILE_DIR}`);let cookies;try{cookies=await readExistingProfileCookies();}catch(e){red('PROFILE_READ_FAILED',e instanceof Error?e.message:String(e));return;}if(!cookies.length){red('PROFILE_SESSION_MISSING','Existing profile exposed no astalavr.com cookies. No login attempt was made.');return;}const pageCookies=cookieHeader(cookies,TARGET);console.log('PROFILE_SESSION=present');console.log(`COOKIE_COUNT=${cookies.length}`);console.log(`PAGE_COOKIE_NAMES=${cookieNames(pageCookies).join(',')||'none'}`);let entry;try{entry=ensureImpit();}catch(e){red('IMPIT_INSTALL_FAILED',e instanceof Error?e.message:String(e));return;}let mod;try{mod=await import(pathToFileURL(entry).href);}catch(e){red('IMPIT_IMPORT_FAILED',e instanceof Error?e.message:String(e));return;}const Impit=mod.Impit||mod.default?.Impit||mod.default;if(typeof Impit!=='function'){red('IMPIT_API_MISMATCH','Impit constructor not found.');return;}const client=new Impit({browser:'chrome'}),pageHeaders={Accept:'text/html,application/xhtml+xml,application/xml;q=0.9,image/avif,image/webp,*/*;q=0.8','Accept-Language':'en-US,en;q=0.9',Referer:'https://astalavr.com/'};if(pageCookies)pageHeaders.Cookie=pageCookies;let page;try{page=await client.fetch(TARGET,{method:'GET',redirect:'follow',headers:pageHeaders});}catch(e){red('PAGE_TRANSPORT_ERROR',e instanceof Error?e.message:String(e));return;}console.log(`PAGE_STATUS=${page.status}`);console.log(`PAGE_CONTENT_TYPE=${page.headers.get('content-type')||'unknown'}`);if(page.status!==200){try{await page.body?.cancel?.();}catch{}red('PAGE_NOT_200','Chrome-impersonated transport could not obtain the designated live page. Do not rerun login automatically.');return;}let html;try{html=await page.text();}catch(e){red('PAGE_BODY_ERROR',e instanceof Error?e.message:String(e));return;}const parsed=parseLivePage(html);console.log(`PROVIDER_ASSET_ID=${parsed.id}`);console.log(`RENDITION_COUNT=${parsed.renditions.length}`);if(!parsed.renditions.length){red('NO_RENDITIONS','Live page returned 200 but no Direct MP4 <source> URL was found.');return;}const media=parsed.renditions[0],mediaCookies=cookieHeader(cookies,media.src);console.log(`LOWEST_RENDITION=${media.quality}`);console.log(`MEDIA_TARGET=${safeUrl(media.src)}`);console.log(`MEDIA_COOKIE_NAMES=${cookieNames(mediaCookies).join(',')||'none'}`);const headers={Accept:'*/*',Range:'bytes=0-0',Referer:TARGET,Origin:'https://astalavr.com','Sec-Fetch-Site':'same-site','Sec-Fetch-Mode':'cors','Sec-Fetch-Dest':'video'};if(mediaCookies)headers.Cookie=mediaCookies;let res;try{res=await client.fetch(media.src,{method:'GET',redirect:'follow',headers});}catch(e){red('MEDIA_TRANSPORT_ERROR',e instanceof Error?e.message:String(e));return;}const cr=res.headers.get('content-range')||'',cl=res.headers.get('content-length')||'';console.log(`MEDIA_STATUS=${res.status}`);console.log(`MEDIA_CONTENT_RANGE=${cr||'missing'}`);console.log(`MEDIA_CONTENT_LENGTH=${cl||'missing'}`);if(res.status!==206){try{await res.body?.cancel?.();}catch{}red('MEDIA_NOT_206','Server did not honor the one-byte Range request; body was not intentionally consumed.');return;}if(!/^bytes\s+0-0\/(\d+)$/i.test(cr)){try{await res.body?.cancel?.();}catch{}red('BAD_CONTENT_RANGE',cr||'missing Content-Range');return;}const bytes=(await res.arrayBuffer()).byteLength;console.log(`MEDIA_BODY_BYTES=${bytes}`);if(bytes!==1){red('BAD_RANGE_BODY_LENGTH',`Expected 1 byte, received ${bytes}`);return;}console.log('RESULT=GREEN');console.log('NEXT=Browser-impersonated HTTP transport is sufficient for page + strict media Range; no CDP bulk media transport is justified.');}
await main();
